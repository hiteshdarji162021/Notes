# Python String rsplit() – Visual Master Guide

---

# 1️⃣ Simple Definition

`rsplit()` means **Right Split**.

✔ Splits the string from the RIGHT side  
✔ Stops after `maxsplit` splits  
✔ Returns a LIST

Syntax:

```
string.rsplit(separator, maxsplit)
```

---

# 2️⃣ Base Long Example (For Better Visibility)

```python
text = "My name is khan and gabar singh"
```

Word Structure:

```
My | name | is | khan | and | gabar | singh
```

Total spaces = 6  
Maximum possible splits = 6

---

# 3️⃣ rsplit(" ", 2)

```python
print(text.rsplit(" ", 2))
```

Output:
```
['My name is khan and', 'gabar', 'singh']
```

Explanation:
- Split from right
- 2 splits only
- Last 2 words separated

---

# 4️⃣ rsplit(" ", 3)

```python
print(text.rsplit(" ", 3))
```

Output:
```
['My name is khan', 'and', 'gabar', 'singh']
```

Explanation:
- Last 3 words separated

---

# 5️⃣ rsplit(" ", 4)

```python
print(text.rsplit(" ", 4))
```

Output:
```
['My name is', 'khan', 'and', 'gabar', 'singh']
```

Explanation:
- Last 4 words separated

---

# 6️⃣ rsplit(" ", 5)

```python
print(text.rsplit(" ", 5))
```

Output:
```
['My name', 'is', 'khan', 'and', 'gabar', 'singh']
```

Explanation:
- Last 5 words separated

---

# 7️⃣ rsplit(" ", 6)

```python
print(text.rsplit(" ", 6))
```

Output:
```
['My', 'name', 'is', 'khan', 'and', 'gabar', 'singh']
```

Explanation:
- All words separated
- Same as full split

---

# 8️⃣ Visual Comparison Table

| maxsplit | Result |
|-----------|--------|
| 2 | ['My name is khan and', 'gabar', 'singh'] |
| 3 | ['My name is khan', 'and', 'gabar', 'singh'] |
| 4 | ['My name is', 'khan', 'and', 'gabar', 'singh'] |
| 5 | ['My name', 'is', 'khan', 'and', 'gabar', 'singh'] |
| 6 | ['My', 'name', 'is', 'khan', 'and', 'gabar', 'singh'] |

---

# 9️⃣ Important Rule

Actual splits = minimum(maxsplit, number_of_separators)

If maxsplit > spaces → full split only

---

# 🔟 Automation Real Use Case

Example: Extract last 2 values from log

```python
log = "TestCase Login PASS 0.45s"
print(log.rsplit(" ", 2))
```

Output:
```
['TestCase Login', 'PASS', '0.45s']
```

Used in:
✔ Extract Status  
✔ Extract Execution Time  
✔ Parse log entries  

---

# 🤖 AI Use Case

Example: Extract confidence score

```python
result = "prediction=spam confidence=0.92"
print(result.rsplit("=", 1))
```

Output:
```
['prediction=spam confidence', '0.92']
```

Used in:
✔ Structured LLM output parsing  
✔ Extract last token  
✔ Separate metadata from message  

---

# 🚀 Final Summary

✔ rsplit() works from RIGHT  
✔ Very useful when last part is important  
✔ Common in logs, filenames, AI structured output  
✔ Safer than split() when you only need last few parts

---

This guide gives clear visibility of rsplit behavior with multiple maxsplit values.

