# What covered in notes

- map(), filter() and reduce(), foreach() some(), find(), every()
- Object destructuring
- Array destructuring
- Default Values assign while Object destructuring
- Default Values assign while Array destructuring
- Rest Operator (...)
- Alias in Object destructuring

## Why we learn function?

- methods - To process and validate collections of data cleanly without manual loops in automation code.
- Object destructuring- To extract required properties from objects in a short, readable way.
- Default Values assign while Object/array destructuring - prevent undefined
- Array destructuring - To assign multiple array values to variables easily and clearly.
- Alias in Object destructuring - To avoid variable name conflicts and improve clarity when property names are long or unclear.

## 1. Array Methods – map(), filter() and reduce(), foreach() some(), find(), every()

---

## 1️⃣ map()

### Purpose

- Transform **each element** of an array
- Always returns a **new array**
- Length of new array = length of original array

### Code Examples

```js
let number = [1, 2, 3, 4, 5];
let newNumber = number.map((ele) => ele + 5);
console.log(newNumber);
```

**Output**

```
[6, 7, 8, 9, 10]
```

---

```js
let number2 = [1, 2, 3, 4, 5];
let newNumber2 = number2.map((ele) => ele * ele);
console.log(newNumber2);
```

**Output**

```
[1, 4, 9, 16, 25]
```

---

```js
let words = ["playwright", "selenium", "cypress"];
let upper = words.map((ele) => ele.toUpperCase());
console.log(upper);
```

**Output**

```
["PLAYWRIGHT", "SELENIUM", "CYPRESS"]
```

---

```js
let words1 = ["playwright", "selenium", "cypress"];
let firstChar = words1.map((ele) => ele[0]);
console.log(firstChar);
```

**Output**

```
["p", "s", "c"]
```

---

```js
let words2 = ["playwright", "selenium", "cypress"];
let conct = words2.map((ele) => "web " + ele);
console.log(conct);
```

**Output**

```
["web playwright", "web selenium", "web cypress"]
```

### Use Cases

- Convert API response data
- Transform test data
- Create derived values

---

## 2️⃣ filter()

### Purpose

- Select elements based on a **condition**
- Returns a **new array** with fewer or equal elements

### Code Examples

```js
let number3 = [1, 2, 3, 4, 5];
let filterNumber = number3.filter((ele) => ele > 3);
console.log(filterNumber);
```

**Output**

```
[4, 5]
```

---

```js
let number32 = [1, 2, 3, 4, 5];
let evennumbers = number32.filter((ele) => ele % 2 === 0);
console.log(evennumbers);
```

**Output**

```
[2, 4]
```

---

```js
let footer = [
  "amazon help",
  "contact",
  "amazon music",
  "amazon prime",
  "price",
];

let amazonlist = footer
  .filter((ele) => ele.startsWith("amazon"))
  .filter((ele) => ele.includes("music"));

console.log(amazonlist);
```

**Output**

```
["amazon music"]
```

### Use Cases

- Search filters
- Valid test data extraction
- Conditional selection

---

## 3️⃣ reduce()

### Purpose

- Reduce array to a **single value**
- Can return number, string, array, object

### Code Examples

```js
let number5 = [1, 2, 3, 4, 5];
let reduceNumber = number5.reduce((result, num) => result + num, 0);
console.log(reduceNumber);
```

**Output**

```
15
```

---

```js
let dict = ["playwright", "selenium", "cypress"];
let newdic = dict.reduce((result, ele) => result + " " + ele);
console.log(newdic);
```

**Output**

```
playwright selenium cypress
```

---

### Combo: map + filter + reduce

```js
let n1 = [85, 92, 67, 78, 95, 43, 88, 76, 91, 82];

let avg =
  n1
    .filter((ele) => ele >= 70)
    .map((ele) => ele + 5)
    .reduce((result, ele) => result + ele, 0) /
  n1.filter((ele) => ele >= 70).length;

console.log(avg);
```

**Output**

```
87.5
```

---

### Use Cases

- Total, average, score calculation
- Report generation
- Data aggregation

---

### `forEach()` – No Return

```js
let product = ["macbook", "samsung", "iphone", "sim", "keyboard"];

product.forEach((ele) => {
  console.log(ele);
});
```

### Output

```
macbook
samsung
iphone
sim
keyboard
```

### `some()` – Returns Boolean

```js
let product1 = ["macbook", "samsung", "iphone", "sim", "keyboard"];

let flag = product1.some((ele) => {
  return ele === "iphone";
});

console.log(flag);
```

### Output

```
true
```

### Use Case

- Search conditions
- Validation logic

---

### `find()` – Find first matching element

```js
let users = [18, 25, 30, 40];

let result = users.find((age) => age > 20);

console.log(result);
```

### Output

```
25
```

### Use Case

- Find first matching record
- Get specific user / item
- Stop search early

---

### `every()` – Check if all elements match condition and return boolean

```js
let marks = [60, 70, 80];

let result = marks.every((m) => m >= 50);

console.log(result);
```

### Output

```
true
```

### Use Case

- Form validation
- Test pass/fail
- Rule enforcement

---

## 🔚 Methods Comparison Table

| Feature  | map             | filter       | reduce          |
| -------- | --------------- | ------------ | --------------- |
| Length   | Same            | Same or less | Single          |
| Mutable  | ❌ No           | ❌ No        | ❌ No           |
| Best for | Data conversion | Conditions   | Totals, reports |

---

## 🔚 Final Comparison Table

| Method  | Returns      | Stops Early | Use          |
| ------- | ------------ | ----------- | ------------ |
| map     | New array    | ❌ No       | Transform    |
| filter  | New array    | ❌ No       | Select       |
| reduce  | Any value    | ❌ No       | Aggregate    |
| find    | Single value | ✅ Yes      | Find one     |
| forEach | Nothing      | ❌ No       | Side effects |
| some    | Boolean      | ✅ Yes      | Any match    |
| every   | Boolean      | ✅ Yes      | All match    |

---

## 2. Destructuring

Destructuring allows **clean extraction** of values from arrays and objects.

---

## Object Destructuring

- Example -1

```js
let user = {
  name: "hitesh",
  age: 33,
  city: "dehgam",
  ispermenent: true,
};

function getInfo({ name, age }) {
  console.log(name);
  console.log(age);
}

getInfo(user);
```

**Output**

```
hitesh
33
```

---

- Example 2

### Use case: Extract data from test config

```js
const testData = {
  username: "admin",
  password: "admin123",
  role: "QA",
};

const { username, password } = testData;

await page.fill("#user", username);
await page.fill("#pass", password);
```

### Why needed

- Cleaner code
- Avoid repeating `testData.` everywhere

---

## Array Destructuring

- Example-1

```js
let products = ["macbook", "keyboard", "imac"];
let [a, b, c] = products;
console.log(a, b, c);
```

**Output**

```
macbook keyboard imac
```

---

### Skip Values

```js
let prod = ["macbook", "keyboard", "imac"];
let [a1, , c1] = prod;
console.log(a1);
console.log(c1);
```

**Output**

```
macbook
imac
```

- Example-2

### Use case: Browser / context / page creation

```js
const browsers = ["chromium", "firefox"];

const [primaryBrowser, secondaryBrowser] = browsers;

console.log(primaryBrowser);
```

### Why needed

- Easy extraction by position
- Clean test setup

---

---

## 4. Default Values assign while Object destructuring

- Example -1

```js
let user1 = {
  name: undefined,
  age: 33,
  ispermenent: true,
};
function getInfo({ name = "ronak", age, city = "nandol" }) {
  console.log(name);
  console.log(age);
  console.log(city);
}

getInfo(user1);
```

**Output**

```
ronak
33
nandol
```

- Example-2

### Use case: Safe config handling

```js
const config = {
  baseURL: "https://test.app",
};

const { baseURL, timeout = 30000 } = config;

await page.goto(baseURL, { timeout });
```

### Why needed

- Prevent `undefined`
- Avoid extra `if` conditions

---

## 3. Default Values assign while Array destructuring

- Example-1

```js
let products33 = ["macbook", "keyboard", "imac"];
let [first = "a", second, third, fourth = "abc"] = products33;
console.log(first, second, third, fourth);
```

**Output**

```
macbook keyboard imac abc
```

- Example-2

### Use case: Fallback browser selection

```js
const browsers = [];

const [browser = "chromium"] = browsers;

console.log(browser);
```

### Why needed

- Safe fallback
- Prevent test crashes

---

## 4. Rest Operator

- Example-1

```js
let number = [1, 2, 3, 4, 5];
let [head, ...tail] = number;
console.log(head);
console.log(tail);
```

**Output**

```
1
[2, 3, 4, 5]
```

- Example-2

```js
const userData = {
  username: "admin",
  password: "admin123",
  token: "xyz",
  role: "QA",
};

const { username, password, ...optionalData } = userData;

console.log(optionalData);
```

### Why needed

- Flexible data handling
- Useful in API + UI hybrid tests

---

### alias in Object destructuring

- Example 1

```js
// Original object
let employee = {
  name: "Hitesh",
  age: 33,
  city: "Dehgam",
  role: "QA Lead",
};

function printEmployee({ name: fullName, role: designation }) {
  console.log(fullName);
  console.log(designation);
}

printEmployee(employee);
```

**Output**

```
Hitesh
QA Lead
```

- Example 2

```js
const apiResponse = {
  user_name: "hitesh",
  user_role: "QA",
};

const { user_name: username, user_role: role } = apiResponse;

console.log(username, role);
```

### Why needed

- Cleaner variable names
- Avoid conflicts
- Align API data with UI naming

---

## Best Practices

- Prefer `map/filter/reduce` over loops for clarity
- Always provide initial value in `reduce`
- Do not mutate original arrays
- Use destructuring in function parameters
- Keep chains readable (avoid very long chains)

---

### Key Takeaway

> **map → change data**  
> **filter → choose data**  
> **reduce → summarize data**
> **foreach()->perform action on same array**
> **some()-> if any condition match, return true**
> **find()-> if first condtion pass, return value**
> **every()-> if all value condition match then only true**

---
